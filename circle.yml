machine:
  node:
    version: 4.0
dependencies:
  override:
    - npm install --dev
    - bundle check --path=vendor/bundle || bundle install --path=vendor/bundle --jobs=4 --retry=3
test:
  override:
    - ./node_modules/.bin/sc-run:
        background: true
    - sleep 25
    - ./test/travis.sh
